import 'package:flutter/material.dart';

class SeperatorWidget extends StatelessWidget {
  const SeperatorWidget({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Image.asset("assets/images/seperator.png");
  }
}
